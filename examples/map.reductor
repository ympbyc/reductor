[ :map ([a] (a -- b) -- [b])
   [ over
     [ drop drop [] ]
    [ swap dup car rot tuck call swap rot cdr swap map ;swap drop swap drop
     swap cons ]
     rot
     empty? if ] define,

   [ 1 2 3 4 5 ] [ 2 * ] map . ]
