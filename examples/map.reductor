[ :double (int -- int)
   [ 2 * ] define,

  :map ([a] (a -- b) -- [b])
   [ over empty?
     [ drop drop [] ]
     [ over car over call swap rot cdr swap map swap cons ]
     if ] define,

   [ 1 2 3 4 5 ] [ double ] map . ]
